name: Route completion invariant evidence

on:
  workflow_dispatch:
    inputs:
      source_sha:
        description: Exact deployed main SHA
        required: true

permissions:
  actions: read
  contents: read
  id-token: write

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Require actor allowlist
        env:
          DEPLOY_ALLOWED_ACTORS: ${{ vars.DEPLOY_ALLOWED_ACTORS }}
          GITHUB_ACTOR_NAME: ${{ github.actor }}
        run: |
          set -euo pipefail
          case ",${DEPLOY_ALLOWED_ACTORS// /}," in *",$GITHUB_ACTOR_NAME,"*) ;; *) exit 1 ;; esac
      - uses: actions/checkout@v6
        with: { fetch-depth: 0, ref: main }
      - name: Require exact successful main SHA
        env:
          GH_TOKEN: ${{ github.token }}
          SOURCE_SHA: ${{ inputs.source_sha }}
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$SOURCE_SHA"
          test "$(gh api "repos/$GITHUB_REPOSITORY/actions/workflows/ci.yml/runs?branch=main&event=push&status=success&head_sha=$SOURCE_SHA&per_page=1" --jq '.total_count')" -gt 0
      - uses: aws-actions/configure-aws-credentials@e7f100cf4c008499ea8adda475de1042d6975c7b
        with:
          role-to-assume: ${{ vars.AWS_ROUTE_OPS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION || 'ap-northeast-2' }}
      - name: Collect read-only production evidence
        env:
          AWS_REGION: ${{ vars.AWS_REGION || 'ap-northeast-2' }}
          SOURCE_SHA: ${{ inputs.source_sha }}
          SSM_TAG_KEY: ${{ vars.SSM_ROUTE_OPS_TARGET_TAG_KEY || 'Service' }}
          SSM_TAG_VALUE: ${{ vars.SSM_ROUTE_OPS_TARGET_TAG_VALUE || 'clever-delivery-server' }}
        run: |
          set -euo pipefail
          read -r count instance_id ping_status <<<"$(aws ssm describe-instance-information --region "$AWS_REGION" --filters "Key=tag:${SSM_TAG_KEY},Values=${SSM_TAG_VALUE}" --query '[length(InstanceInformationList), InstanceInformationList[0].InstanceId, InstanceInformationList[0].PingStatus]' --output text)"
          test "$count" = 1
          test "$ping_status" = Online
          command="cd /srv/clever-route-server && lock=.deploy/route-ops-simple-deploy.lock.d && mkdir \"\$lock\" && trap 'rmdir \"\$lock\"' EXIT && container=clever-route-clever-route-api-1 && expected_image=\$(sed -n 's/^DELIVERY_API_IMAGE=//p' .deploy/current-image.env) && test \"\$(docker inspect \"\$container\" --format '{{.Config.Image}}')\" = \"\$expected_image\" && test \"\$(docker inspect \"\$container\" --format '{{ index .Config.Labels \"org.opencontainers.image.revision\" }}')\" = '$SOURCE_SHA' && test \"\$(docker inspect \"\$container\" --format '{{.Image}}')\" = \"\$(docker image inspect \"\$expected_image\" --format '{{.Id}}')\" && docker compose -p clever-route --env-file .deploy/current-image.env -f infra/compose/docker-compose.prod.yml exec -T clever-route-api node -e \"fetch('http://127.0.0.1:3000/healthz').then(r=>process.exit(r.status===200?0:1)).catch(()=>process.exit(1))\" && docker compose -p clever-route --env-file .deploy/current-image.env -f infra/compose/docker-compose.prod.yml exec -T clever-route-api node dist/scripts/report-driver-route-completion-invariant.js --source-sha '$SOURCE_SHA'"
          parameters="$(jq -cn --arg command "$command" '{commands:["bash -lc " + ($command|@sh)]}')"
          command_id="$(aws ssm send-command --region "$AWS_REGION" --instance-ids "$instance_id" --document-name AWS-RunShellScript --comment "Read-only route completion evidence at $SOURCE_SHA" --parameters "$parameters" --query Command.CommandId --output text)"
          aws ssm wait command-executed --region "$AWS_REGION" --command-id "$command_id" --instance-id "$instance_id"
          test "$(aws ssm get-command-invocation --region "$AWS_REGION" --command-id "$command_id" --instance-id "$instance_id" --query ResponseCode --output text)" = 0
          aws ssm get-command-invocation --region "$AWS_REGION" --command-id "$command_id" --instance-id "$instance_id" --query StandardOutputContent --output text > route-completion-rollout-evidence.json
          jq -e --arg sha "$SOURCE_SHA" '.sourceSha == $sha and (.currentMode | IN("OBSERVE", "GUARDED", "FULL"))' route-completion-rollout-evidence.json
      - uses: actions/upload-artifact@v7
        with:
          name: route-completion-rollout-evidence
          path: route-completion-rollout-evidence.json
          if-no-files-found: error
          retention-days: 14
