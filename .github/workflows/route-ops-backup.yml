name: Route Ops backup setup

on:
  workflow_dispatch:
    inputs:
      source_ref:
        description: "Git ref containing the backup worker and installer"
        required: true
        default: main
      dry_run:
        description: "Validate host prerequisites without installing the timer"
        required: true
        type: boolean
        default: true
      run_backup:
        description: "Run one verified backup after installing the timer"
        required: true
        type: boolean
        default: false

permissions:
  contents: read
  id-token: write

concurrency:
  group: route-ops-backup-setup
  cancel-in-progress: false

jobs:
  setup:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Require main branch
        if: github.ref != 'refs/heads/main'
        run: exit 1

      - name: Require actor allowlist
        shell: bash
        env:
          DEPLOY_ALLOWED_ACTORS: ${{ vars.DEPLOY_ALLOWED_ACTORS }}
          GITHUB_ACTOR_NAME: ${{ github.actor }}
        run: |
          set -euo pipefail
          IFS=',' read -ra actors <<< "$DEPLOY_ALLOWED_ACTORS"
          for actor in "${actors[@]}"; do
            if [ "$(echo "$actor" | xargs)" = "$GITHUB_ACTOR_NAME" ]; then exit 0; fi
          done
          echo "$GITHUB_ACTOR_NAME is not allowed to configure Route Ops backups." >&2
          exit 1

      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ inputs.source_ref }}

      - name: Require source on main history
        run: |
          git fetch --no-tags origin main
          git merge-base --is-ancestor HEAD origin/main

      - name: Validate backup lane
        run: |
          chmod +x scripts/backup-route-ops-data.sh scripts/ssm-install-route-ops-backup.sh tests/deploy/route-ops-backup.test.sh
          tests/deploy/route-ops-backup.test.sh

      - name: Configure AWS credentials through OIDC
        uses: aws-actions/configure-aws-credentials@e7f100cf4c008499ea8adda475de1042d6975c7b
        with:
          role-to-assume: ${{ vars.AWS_ROUTE_OPS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ vars.AWS_REGION || 'ap-northeast-2' }}

      - name: Configure backup through SSM
        shell: bash
        env:
          AWS_REGION: ${{ vars.AWS_REGION || 'ap-northeast-2' }}
          ROUTE_OPS_BACKUP_SSM_TAG_KEY: ${{ vars.SSM_ROUTE_OPS_TARGET_TAG_KEY || 'Service' }}
          ROUTE_OPS_BACKUP_SSM_TAG_VALUE: ${{ vars.SSM_ROUTE_OPS_TARGET_TAG_VALUE || 'clever-delivery-server' }}
        run: |
          set -euo pipefail
          args=()
          if [ "${{ inputs.dry_run }}" = true ]; then args+=(--dry-run); fi
          if [ "${{ inputs.run_backup }}" = true ]; then args+=(--run-backup); fi
          scripts/ssm-install-route-ops-backup.sh "${args[@]}"
